---
phase: 3
plan: 3
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - scripts/verify-exhibition-pages.mjs
  - tests/phase1-shell.spec.mjs
  - package.json
  - src/components/SiteHeader.astro
requirements:
  - EXH-01
  - EXH-19
autonomous: true
must_haves:
  goal: "Navigation and automated checks prove the browseable exhibition flow exists."
  requirements:
    - EXH-01
    - EXH-19
  verification:
    - "npm run verify:pages exits 0"
    - "npm run verify exits 0"
---

# Plan 03-03: Add Navigation Verification and Visual Test Updates

## Tasks

- Add `scripts/verify-exhibition-pages.mjs` to inspect `dist/` after build.
- Verify the homepage links to every manifest exhibit route.
- Verify every built exhibit page contains the title, overview link, and previous/next navigation where applicable.
- Add `verify:pages` npm script and include it in `verify`.
- Update the Playwright visual shell check to use a real exhibit route, not `/exponate/platzhalter/`.
- Update header navigation so `Exponate` points to the exhibit grid on `/`.
